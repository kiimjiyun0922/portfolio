# 프로젝트 작업 지침

## 공개 프론트·테마 작업 전 필수 확인

공개 포트폴리오 프론트, 테마, 테마 가이드, 반응형 미리보기를 만들거나 수정하기 전에 반드시 `docs/FRONT_THEME_CONTRACT.md`를 먼저 읽는다. 이 계약은 어드민 UI에 적용하지 않으며, 어드민 파일과 `docs/DESIGN_SYSTEM.md`는 별도 작업 범위로 유지한다.

새로 작성되거나 갱신된 가이드가 기존 CSS, 이전 화면, 과거 캡처, 오래된 컴포넌트보다 항상 우선한다. 충돌이 생기면 새 가이드를 약화하지 않고 기존 구현을 수정한다.

프론트 수정은 다음 순서를 지킨다.

1. 해당 테마의 핵심 은유, 정보 구조, 표면·선, 타이포그래피, 상호작용, 금지 요소를 먼저 분석한다.
2. 새 표현은 분석에서 필요성이 증명된 경우에만 추가한다. 다른 테마의 장식과 컴포넌트를 복제하지 않는다. 공용 Mist형 레이아웃에 색·서체·radius만 교체한 구현은 테마로 인정하지 않는다.
3. 공통 시작선, 대칭 여백, 광학 정렬 토큰을 사용한다. 개별 화면에 임의 margin이나 픽셀 보정을 추가하지 않는다.
4. 같은 패턴을 사용하는 모든 테마·화면·상태를 함께 수정한다.
5. 320·360·390·430·768·1024·1280·1440px에서 오버플로, 줄바꿈, 터치 영역, 텍스트 실제 좌표를 검수한다.

## 공개 프론트 반복 금지 품질 게이트

1. 한 줄 행의 번호·메타·제목·상태 라벨은 글자 실제 중심 오차가 1px 이하여야 한다.
2. 여러 줄 행은 번호와 제목 첫 줄의 실제 글자 상단 오차가 2px 이하여야 한다.
3. 행·카드의 상하 padding 차이는 1px 이하여야 한다. 고정 높이와 빈 줄로 균형을 만들지 않는다.
4. 데이터 묶음은 첫 상단선과 마지막 하단선을 모두 가진다. 인접 선은 한 번만 그린다.
5. 날짜·번호·메타는 주제목보다 작게 유지하며 문맥마다 임의로 확대하지 않는다.
6. hover·focus·pressed에서 위치·크기·padding을 바꾸지 않는다. focus 표시는 한 겹만 사용한다.
7. 테마 고유색, 서체, 선, 모서리, 표면, 모션은 해당 테마 분석에 근거한다. Mist의 종이·포그·반짝이·랜덤 커서를 다른 테마에 전이하지 않는다.
8. 가이드 자체도 실제 테마와 같은 규칙 및 자동 검수를 통과해야 한다.

## 어드민 UI 작업 전 필수 확인

어드민 인터페이스를 만들거나 수정하기 전에 `docs/DESIGN_SYSTEM.md`와 `#admin-system`의 현재 규칙을 먼저 확인한다. 한 컴포넌트의 문제를 고칠 때는 같은 패턴을 사용하는 모든 화면과 상태를 함께 검색하고 검수한다. 지적된 화면 하나만 덮는 예외 CSS를 추가하지 않는다.

## 반복 금지 품질 게이트

다음 항목은 모든 어드민 UI 변경에서 반드시 확인한다.

1. 광학 정렬: 번호와 제목처럼 크기가 다른 글자는 박스 상단 좌표가 아니라 첫 텍스트 베이스라인을 맞춘다. 아이콘은 글자와 시각 중심을 맞추고 보정은 공통 컴포넌트에만 적용한다.
2. 공통 시작선: 번호, 제목, 설명, 입력 필드와 액션은 문서화된 그리드 시작선을 공유한다. 계단식 정렬이나 컴포넌트별 임의 마진을 만들지 않는다.
3. 중앙 정렬: 데모나 빈 상태처럼 콘텐츠보다 영역이 큰 프레임은 콘텐츠 묶음을 가로·세로 중앙에 둔다. 긴 편집 콘텐츠와 데이터 표는 상단 정렬한다.
4. 타이포 역할: 글자 크기는 문서의 역할 토큰만 사용한다. Page 32/40(모바일 28/36), Section 18/28, Component 16/24, Body 16/26, Control 15/22, Support 14/22, Micro 13/20을 사용하며 13px 미만은 금지한다. 공간에 맞추려고 임의 px 값을 만들지 말고 허용된 Micro 13/20 역할로 한 단계만 낮춘다.
5. 버튼 형태: 라벨은 가로 한 줄을 기본으로 하며 세로로 길쭉한 버튼이나 글자 단위 줄바꿈을 허용하지 않는다. 아이콘 버튼은 정사각형, 텍스트 버튼은 공통 높이와 좌우 패딩을 사용한다.
6. 입력과 선택: 정해진 값은 select, 날짜는 date/calendar, 색상은 picker와 코드 입력을 함께 제공한다. 드롭다운 화살표는 우측 중앙에 고정하고 중첩 필드의 포커스 선은 한 겹만 보이게 한다.
7. 실제 컴포넌트: 카탈로그에는 추상 도형이 아니라 실제 라벨, 컨트롤, 상태를 가진 구현 모양을 표시한다.
8. 반응형: 360px, 768px, 1280px에서 확인한다. 페이지 전체 가로 오버플로는 금지하고 표처럼 필요한 영역만 내부 스크롤을 허용한다. 태블릿 내비게이션은 번호만 남기지 않는다.
9. 라이트·다크: 두 모드에서 배경, 텍스트, 경계, 포커스, 오버레이 대비를 확인한다. 하드코딩 색상 대신 어드민 토큰을 사용한다.
10. 상태 완결성: default, hover, focus, disabled, loading, empty, error, success와 오버레이 닫기 동작을 함께 검수한다.
11. 테마 관리 분리: 어드민 Light/Dark와 방문자 포트폴리오 테마를 혼동하지 않는다. 테마 선택 화면과 실제 화면 미리보기 툴바를 관리 컴포넌트로 정의하고 공개 사이트의 IA는 테마가 바꾸지 못한다.
12. 데이터 안전: 저장, JSON 가져오기·내보내기, 개별 편집, 삭제·복원 흐름을 유지한다. 가져오기는 검증과 차이 미리보기를 거친다.
13. 전역 점검: 하나의 문제를 수정한 뒤 같은 클래스, 같은 컴포넌트군, 라이트·다크, 세 반응형 폭을 모두 재검사한다.
14. 작업 폭 기준 반응형: 어드민 폼의 열 수는 브라우저 전체 폭이 아니라 사이드바와 페이지 패딩을 제외한 실제 편집 영역을 기준으로 정한다. 768px 태블릿에서 데스크톱용 2·3열 폼을 강제로 유지하지 않는다.
15. 오버레이 독립성: 모달·드로어는 sticky, transform, filter, backdrop-filter가 있는 툴바 안에 렌더링하지 않는다. 문서 루트 포털에서 뷰포트를 기준으로 배치한다.
16. 문서형 폼과 복합 기간: 단일 섹션 편집 화면의 필드를 전체 폭 한 열로 나열하지 않고 의미 단위 패널과 작업 폭 기반 1·2열 그리드로 묶는다. 시작월·종료월·현재 체크가 있는 기간 입력은 일반 필드용 3등분 열에 넣지 않고 충분한 전체 행을 사용한다.
17. 테마의 암묵 적용: 공개 콘텐츠 편집 화면에는 현재 테마 안내, 팔레트 견본이나 별도 테마 선택을 반복하지 않는다. 저장한 방문자 테마는 공개 화면에 자동 적용되고 콘텐츠 편집기는 구조와 값만 다룬다.
18. 프로젝트의 중립성: `PM`, `디자인`처럼 직무나 분야를 프로젝트의 최상위 정보 구조로 고정하지 않는다. 구조형·아카이브형 등은 프로젝트를 보여주는 표현 방식이며, 새 방식은 같은 프로젝트 메뉴 안에서 확장한다. 기존 저장 키는 데이터 호환을 위해 유지할 수 있지만 사용자에게 분류명으로 노출하지 않는다.
19. 버튼 상태 대비: primary·secondary·quiet·danger·icon·link는 default, hover, focus, pressed, disabled, loading의 배경·글자·테두리를 한 세트로 검수한다. hover에서 글자만 연하게 하거나 primary 채움을 지우지 않고, 위치·크기도 바꾸지 않는다.
20. 플로팅 조작부: 44px 정사각형 버튼을 하나의 중성 표면에 묶고 safe area를 확보한다. Light·Dark 모두 아이콘과 경계 대비를 확인하며 본문과 sticky 조작부를 가리지 않는다.
21. 파괴 행동 확인: 삭제, 기록 제거, 이미지 비우기와 연결 해제는 모두 대상과 영향을 설명하는 시스템 다이얼로그를 먼저 표시한다. 편집 중 삭제는 저장 전임을 알리고 브라우저 기본 confirm이나 즉시 삭제를 쓰지 않는다.

## 완료 조건

- `npm run check`가 통과해야 한다.
- 인앱 브라우저에서 변경 화면을 실제로 열어 데스크톱과 모바일을 확인한다.
- 광학 정렬은 요소 박스의 top 값이 아니라 텍스트 베이스라인과 실제 스크린샷으로 판단한다.
- 유지되어야 할 새 규칙은 구현과 `docs/DESIGN_SYSTEM.md`에 함께 기록한다.
- 운영 빌드의 `index.html`, `robots.txt`, `sitemap.xml`에 샘플 이름·도메인·미치환 토큰이 남지 않았는지 확인한다. 사이트 식별 정보는 환경 변수와 저장 데이터에서만 가져온다.
- 각 관리 화면에서 실제 편집 영역 밖으로 잘리거나 숨겨지는 필드가 없는지 DOM 경계와 스크린샷을 함께 확인한다.
